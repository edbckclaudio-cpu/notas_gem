services:
  app:
    build: .
    ports:
      - "3005:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      - exports:/app/public/exports
    restart: unless-stopped

volumes:
  exports: